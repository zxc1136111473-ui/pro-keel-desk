# Harness 0.1.2-alpha.3 升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 dsh-desktop 从锁定的 Harness `dsh-v0.1.2-alpha.1` 升级到 `dsh-v0.1.2-alpha.3`，依赖、补丁、宿主脚本、测试、文档全部适配并通过完整验证。

**Architecture:** 上游未发布 npm，继续用仓库内 `file:` tarball 桥接：在本地克隆 `/Users/alex/Documents/Code/AI/deepseek-harness` 构建并打包 alpha.3 产物，提交进 `packages/harness-0.1.2-alpha.3/`，逐项切换 `package.json` 与 lockfile；16 个 `patch-package` 补丁逐个 rebase 到新构建产物并按「上游是否已原生提供」决定保留 / 删除；宿主侧（`scripts/`、`src/main/`）按 alpha.3 实际行为复核。

**Tech Stack:** Electron + electron-vite，Vitest，patch-package，npm（应用侧），pnpm@11.7.0 via corepack（上游构建侧），Node 24。

## Global Constraints

- 目标上游标签：`dsh-v0.1.2-alpha.3`（commit `dd6322d604e00eec1ba5e0c8541159906a21094a`）。跳过 alpha.2，直接对比 alpha.1 → alpha.3。
- 依赖来源：仓库内 `file:packages/harness-0.1.2-alpha.3/...` tarball，**不**换回语义化版本（列入「剩余工作」）。
- 上游构建目录：`/Users/alex/Documents/Code/AI/deepseek-harness`（已 fetch alpha.3 标签）。
- 构建工具链：`corepack pnpm`（`packageManager: pnpm@11.7.0`）；`pnpm run release:pack -- --family X` 会把参数当位置参数报错，必须直接 `corepack pnpm exec tsx scripts/release/pack.ts --family X`。
- 验证必须用完整 `npm ci`，**禁止** `--ignore-scripts`（会跳过 `postinstall`，漏掉启动期问题）。
- 保持排除上游未启用的 provider / 测试包（e2b、lsp、mock-server、replay、claude-code/codex subagent 等），见 `test/release.test.ts` `excludedHarnessPackages`。
- 补丁判断标准：能力「上游是否已原生提供」→ 是则删补丁并在升级文档记「交还上游」；不是「补丁能不能修好」。
- 每个补丁文件命名 `@deepseek-ai+<pkg>+0.1.2-alpha.3.patch`（版本号跟随依赖）。
- 分支：`claude/harness-alpha3-adapt-adfd61`（当前 worktree，已存在）。
- 完成定义：`npm ci` + `npm run build` + `npm test` + `npm run typecheck` + `node scripts/verify-harness-auth.mjs` + 实际启动 Electron（profile 四个桌面插件全挂载）全部通过。

---

## 文件结构

**上游构建产物（新增目录）**
- `packages/harness-0.1.2-alpha.3/README.md` — 来源标签、构建命令、产物计数
- `packages/harness-0.1.2-alpha.3/npm-vendor/*.tgz` — cordis / cosmokit / schemastery 等
- `packages/harness-0.1.2-alpha.3/npm-dsh/*.tgz` — `@deepseek-ai/dsh-*` 全量
- **删除** `packages/harness-0.1.2-alpha.1/` 整个目录

**依赖 / lockfile**
- `package.json` — 全部 `file:` 路径版本切换；删 `dsh-agent-spine-demo`；按闭包结论增删；`market-installer` peer
- `packages/dsh-desktop-market-installer/package.json` — peerDependencies 范围 → `^0.1.2-alpha.3`
- `package-lock.json` — 重新生成

**补丁（`patches/`，逐个改名 + rebase）**
- `@deepseek-ai+cordis-plugin-loader+*.patch`
- `@deepseek-ai+dsh+*.patch`（闭包 manifest 注入 + spine-demo 移除 + util 新增）
- `@deepseek-ai+dsh-api-session-controller+*.patch`
- `@deepseek-ai+dsh-client-modules+*.patch`
- `@deepseek-ai+dsh-client-ui-agent-preset+*.patch`
- `@deepseek-ai+dsh-client-ui-chat+*.patch`
- `@deepseek-ai+dsh-client-ui-deliverables+*.patch`
- `@deepseek-ai+dsh-client-ui-directory-picker-native+*.patch`
- `@deepseek-ai+dsh-client-ui-layout+*.patch`
- `@deepseek-ai+dsh-client-ui-model-selection+*.patch`
- `@deepseek-ai+dsh-client-ui-settings-models+*.patch`
- `@deepseek-ai+dsh-client-ui-sidebar+*.patch`
- `@deepseek-ai+dsh-client-ui-trajectory+*.patch`
- `@deepseek-ai+dsh-client-ui-workspace+*.patch`
- `@deepseek-ai+dsh-llm-deepseek+*.patch`
- `@deepseek-ai+dsh-llm-pi-ai+*.patch`

**宿主脚本 / 主进程**
- `scripts/install-brand-assets.mjs` — alpha.3 前端资源匹配复核
- `scripts/verify-harness-auth.mjs` — 版本字样 / 行为复核
- `src/main/runtime/harness-runtime.ts` — 令牌捕获、stderr 就绪探测、`CORE_BUNDLES`、注释版本号
- `src/main/window-navigation.ts` — 首导航令牌注释 / 逻辑
- `src/main/mobile/lan-mobile-bridge.ts` — 桥接端点 / 认证换取

**测试（硬编码 alpha.1 的用例）**
- `test/release.test.ts` `test/version-info.test.ts` `test/desktop-plugin-closure.test.ts`
- `test/query-rail-patch.test.ts` `test/branding-patch.test.ts` `test/client-modules-resolution-patch.test.ts`
- `test/preset-transfer-patch.test.ts` `test/directory-picker.test.ts` `test/profile-compatibility.test.ts`
- `test/provider-error-patch.test.ts` `test/open-in-finder.test.ts`

**文档**
- `docs/harness-0.1.2-alpha.3-upgrade.md` — 新增
- `docs/harness-0.1.2-upgrade.md` — 顶部加历史指向
- `README.md` `README.zh.md` `README.ja.md` `README.es.md` `README.pt.md` `README.ru.md` — 版本字样

---

## Task 1: 上游 alpha.3 构建与打包

**Files:**
- Create: `packages/harness-0.1.2-alpha.3/README.md`, `packages/harness-0.1.2-alpha.3/npm-vendor/`, `packages/harness-0.1.2-alpha.3/npm-dsh/`
- Delete: `packages/harness-0.1.2-alpha.1/`

**Interfaces:**
- Produces: `packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-<pkg>-0.1.2-alpha.3.tgz` 与 `npm-vendor/<vendor>-<ver>.tgz` 全量 tarball，供 Task 2 的 `package.json` `file:` 引用。

- [ ] **Step 1: 切到 alpha.3 并装依赖**

```bash
cd /Users/alex/Documents/Code/AI/deepseek-harness
git checkout dsh-v0.1.2-alpha.3
corepack enable
corepack pnpm install --frozen-lockfile
```

Expected: install 成功，`pnpm --version` 报告 11.7.0。

- [ ] **Step 2: 构建 official 客户端**

```bash
corepack pnpm run build:official
```

Expected: 退出 0；生成 official 客户端构建记录（`release:pack` 依赖它）。

- [ ] **Step 3: 打包 vendor 与 dsh 两个 family**

```bash
corepack pnpm exec tsx scripts/release/pack.ts --family vendor --out dist/npm-vendor --concurrency 8
corepack pnpm exec tsx scripts/release/pack.ts --family dsh    --out dist/npm-dsh    --concurrency 8
```

Expected: `dist/npm-vendor/` 与 `dist/npm-dsh/` 产出 `.tgz`。记录两目录的文件数（alpha.1 为 9 vendor + 241 dsh，alpha.3 因新增/删除包会有出入）。

- [ ] **Step 4: 拷贝产物进本仓库并删除旧目录**

```bash
cd /Users/alex/Documents/Code/dataelem/dsh-desktop/.claude/worktrees/harness-alpha3-adapt-adfd61
rm -rf packages/harness-0.1.2-alpha.1
mkdir -p packages/harness-0.1.2-alpha.3
cp -R /Users/alex/Documents/Code/AI/deepseek-harness/dist/npm-vendor packages/harness-0.1.2-alpha.3/
cp -R /Users/alex/Documents/Code/AI/deepseek-harness/dist/npm-dsh    packages/harness-0.1.2-alpha.3/
```

- [ ] **Step 5: 写 README**

`packages/harness-0.1.2-alpha.3/README.md` 内容照 alpha.1 结构：上游标签 `dsh-v0.1.2-alpha.3`、上面 4 步命令、产物计数、「上游发布 npm 后删除本目录」。

- [ ] **Step 6: 生成两个 family 的包清单差异记录（供后续任务参考）**

```bash
comm -3 \
  <(ls /Users/alex/Documents/Code/dataelem/dsh-desktop/packages/harness-0.1.2-alpha.1/npm-dsh 2>/dev/null | sed 's/-0.1.2-alpha.1.tgz//' | sort) \
  <(ls packages/harness-0.1.2-alpha.3/npm-dsh | sed 's/-0.1.2-alpha.3.tgz//' | sort) > /tmp/alpha3-pkg-delta.txt
cat /tmp/alpha3-pkg-delta.txt
```

（alpha.1 目录此时已删，改用 git：`git show HEAD:packages/harness-0.1.2-alpha.1/... ` 或从 `package.json` 历史取列表。）
预期至少包含：删 `deepseek-ai-dsh-agent-spine-demo`、删 `deepseek-ai-dsh-session-persistence-sqlite`（本就未引用）；新增 `deepseek-ai-dsh-client-ui-schedule`、`deepseek-ai-dsh-util-deque`/`-time`/`-values`（实际名以 tarball 为准）。

- [ ] **Step 7: Commit**

```bash
git add packages/harness-0.1.2-alpha.3 packages/harness-0.1.2-alpha.1
git commit -m "chore(harness): 打包上游 dsh-v0.1.2-alpha.3 本地 tarball"
```

---

## Task 2: package.json 依赖切换与 lockfile 重建

**Files:**
- Modify: `package.json`（`dependencies` 全部 harness `file:` 行）
- Modify: `packages/dsh-desktop-market-installer/package.json`
- Regenerate: `package-lock.json`

**Interfaces:**
- Consumes: Task 1 的 `packages/harness-0.1.2-alpha.3/**/*.tgz`。
- Produces: 可 `npm install` 解析的依赖图，供 Task 3+ 的 `npm ci`。

- [ ] **Step 1: 批量替换路径与版本**

把 `package.json` 中每处
`file:packages/harness-0.1.2-alpha.1/npm-dsh/deepseek-ai-<x>-0.1.2-alpha.1.tgz`
→ `file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-<x>-0.1.2-alpha.3.tgz`；
`npm-vendor` 段按 tarball 实际版本号替换（vendor 版本号可能未变，路径仍要改 `alpha.1`→`alpha.3`）。

- [ ] **Step 2: 删除上游已移除的依赖行**

删掉 `"@deepseek-ai/dsh-agent-spine-demo": ...`（`package.json:60` 附近）。
若 Step 6/Task 1 的 delta 显示还有其他被删包出现在 dependencies，一并删除。

- [ ] **Step 3: 加入新增闭包包（若需要）**

用 Task 4 的闭包结论；若此刻未知，先跳过，Task 4 回填后重跑本任务的 lockfile 步骤。

- [ ] **Step 4: 放宽 market-installer peer 范围**

`packages/dsh-desktop-market-installer/package.json` 的 `peerDependencies` 中
`@deepseek-ai/dsh*` 范围 → `^0.1.2-alpha.3`（预发布 caret 只在同一 `major.minor.patch` 内匹配）。

- [ ] **Step 5: 重新生成 lockfile**

```bash
rm -rf node_modules package-lock.json
npm install --ignore-scripts
```

Expected: 生成新的 `package-lock.json`，所有 `@deepseek-ai/*` 指向 `packages/harness-0.1.2-alpha.3/`。

- [ ] **Step 6: 完整 ci 验证依赖可解析**

```bash
rm -rf node_modules
npm ci
```

Expected: 退出 0，`postinstall`（`patch-package` + `install-brand-assets` + `install-electron`）此时可能因补丁未适配而失败 —— 若失败在 `patch-package` 阶段属预期，记录失败补丁名，进入 Task 5；若失败在 `install-brand-assets`，进入 Task 6。仅当失败原因是依赖缺失才在本任务修。

- [ ] **Step 7: Commit**

```bash
git add package.json package-lock.json packages/dsh-desktop-market-installer/package.json
git commit -m "chore(deps): 依赖切换到 harness 0.1.2-alpha.3"
```

---

## Task 3: `@deepseek-ai/dsh` 依赖闭包补丁

**Files:**
- Modify: `patches/@deepseek-ai+dsh+0.1.2-alpha.1.patch` → 改名 `patches/@deepseek-ai+dsh+0.1.2-alpha.3.patch`
- Test: `test/desktop-plugin-closure.test.ts`

**Interfaces:**
- Consumes: Task 1 的 dsh tarball（含 `package.json` manifest）。
- Produces: 注入了 `dsh-desktop-*` 桌面插件的 dsh manifest，profile 解析依赖它。

- [ ] **Step 1: 展开 alpha.3 的 dsh 包 manifest**

```bash
mkdir -p /tmp/dsh-a3 && tar -xzf packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-0.1.2-alpha.3.tgz -C /tmp/dsh-a3
cat /tmp/dsh-a3/package/package.json
```

记录其 `dependencies` 键集合，与 alpha.1 版本（`git show HEAD~2:patches/@deepseek-ai+dsh+0.1.2-alpha.1.patch`）对比。

- [ ] **Step 2: 运行现有闭包测试确认它失败**

```bash
npx vitest run test/desktop-plugin-closure.test.ts
```

Expected: FAIL（补丁文件名还是 alpha.1，或 manifest 行号漂移）。

- [ ] **Step 3: 重建补丁**

```bash
git mv patches/@deepseek-ai+dsh+0.1.2-alpha.1.patch patches/@deepseek-ai+dsh+0.1.2-alpha.3.patch
rm -rf node_modules/@deepseek-ai/dsh && npm ci --ignore-scripts
# 手动编辑 node_modules/@deepseek-ai/dsh/package.json：
#   - 加回 dsh-desktop-client-ui / dsh-desktop-preset-transfer / dsh-desktop-hmr-fallback / dsh-desktop-market-installer（保持 alpha.1 补丁里那份清单）
#   - 若 alpha.1 补丁还改了别的文件（行号偏移），同步
#   - 删除 spine-demo 相关行（若上游 manifest 已无则无需动）
npx patch-package @deepseek-ai/dsh --patch-dir patches
```

Expected: 生成 `patches/@deepseek-ai+dsh+0.1.2-alpha.3.patch`。

- [ ] **Step 4: 若测试要求同步 package.json / dsh 闭包三处一致，更新测试夹具**

`test/desktop-plugin-closure.test.ts` 锁定不变量：profile patch 里每个 `dsh-desktop-*` 行必须同时在 dsh 依赖补丁与 `package.json` dependencies 里。若测试内硬编码 `0.1.2-alpha.1` 路径，改为 `0.1.2-alpha.3`。

- [ ] **Step 5: 跑测试确认通过**

```bash
npx vitest run test/desktop-plugin-closure.test.ts
```

Expected: PASS。

- [ ] **Step 6: Commit**

```bash
git add patches/ test/desktop-plugin-closure.test.ts
git commit -m "fix(patch): dsh 闭包补丁适配 alpha.3"
```

---

## Task 4: 新增 `util/*` 与 `client/ui-schedule` 闭包判定

**Files:**
- Modify（按需）: `package.json`, `patches/@deepseek-ai+dsh+0.1.2-alpha.3.patch`, `test/release.test.ts`

**Interfaces:**
- Consumes: Task 1 tarball，Task 3 的 dsh manifest。
- Produces: 明确结论——新增包是否进 dependencies / 排除清单；lockfile 中 `@deepseek-ai/dsh-*` 全部 `file:` 解析。

**Task 2 审查发现（硬约束，本任务必须消除）：**
Task 2 重建的 lockfile 里，5 个上游新增包 `dsh-deque`、`dsh-util-time`、`dsh-util-values`、
`dsh-client-ui-schedule`、`dsh-session-turn-outline` 目前从 `registry.npmmirror.com`
远端解析（它们是已交付包的传递依赖，但 package.json 未引用 → npm 走网络）。
这违反「依赖来源 = 仓库内 `file:` tarball」全局约束，离线 / 纯仓库环境会失败。
它们的 tarball 其实已在 `packages/harness-0.1.2-alpha.3/npm-dsh/`。

- [ ] **Step 1: 确认这 5 个包确为交付闭包的传递依赖**

```bash
mkdir -p /tmp/dsh-a3 && tar -xzf packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-0.1.2-alpha.3.tgz -C /tmp/dsh-a3
grep -rE "dsh-deque|dsh-util-time|dsh-util-values|dsh-client-ui-schedule|dsh-session-turn-outline" packages/harness-0.1.2-alpha.3/npm-dsh/*/package.json 2>/dev/null || true
# 逐个解包相关 tarball 查 dependencies，确认谁在依赖它们
npm ls @deepseek-ai/dsh-deque @deepseek-ai/dsh-util-time @deepseek-ai/dsh-util-values @deepseek-ai/dsh-client-ui-schedule @deepseek-ai/dsh-session-turn-outline 2>&1 | head -40
```

- [ ] **Step 2: 把 5 个包加入 `package.json` dependencies 为 `file:` 依赖**

对每个包加一行（按字母序插入到 `dependencies` 中对应位置）：
```
"@deepseek-ai/dsh-deque": "file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-deque-0.1.2-alpha.3.tgz",
"@deepseek-ai/dsh-util-time": "file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-util-time-0.1.2-alpha.3.tgz",
"@deepseek-ai/dsh-util-values": "file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-util-values-0.1.2-alpha.3.tgz",
"@deepseek-ai/dsh-client-ui-schedule": "file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-client-ui-schedule-0.1.2-alpha.3.tgz",
"@deepseek-ai/dsh-session-turn-outline": "file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-session-turn-outline-0.1.2-alpha.3.tgz",
```
先 `ls packages/harness-0.1.2-alpha.3/npm-dsh/ | grep -E 'deque|util-time|util-values|ui-schedule|turn-outline'` 核对确切文件名（Task 1 报告：`dsh-deque` 而非 `dsh-util-deque`）。

- [ ] **Step 3: 重建 lockfile 并验证无 registry 解析**

```bash
rm -f package-lock.json && npm install --ignore-scripts
grep -nE '"resolved": "https?://[^"]*deepseek-ai[/-]dsh' package-lock.json
```
Expected: 上面 grep **无输出**（或仅剩 Task 2 前就存在的非本次条目，需逐个说明）。
`grep -c "harness-0.1.2-alpha.1" package-lock.json` = 0。

- [ ] **Step 3b: 完整 `npm ci` 回归**

```bash
rm -rf node_modules && npm ci
```
Expected: 退出 0（patch-package 仍会报 Task 5 未适配的补丁，属预期）。

- [ ] **Step 4: 清理 `session-persistence-sqlite` 排除项**

`test/release.test.ts` `excludedHarnessPackages` 中 `@deepseek-ai/dsh-session-persistence-sqlite`：上游已删包，保留断言仍成立但语义过时。确认 `dsh-storage-sqlite` 是否仍作为 `session-query-sqlite` 的传递依赖存在于 lockfile —— 若存在则保留排除项，否则一并说明。按实际情况决定删除或加注释。

- [ ] **Step 5: 跑 release + 闭包测试**

```bash
npx vitest run test/release.test.ts test/desktop-plugin-closure.test.ts
```

Expected: PASS（`release.test.ts` 现在还会校验 5 个新增包已作为 `file:` 依赖列出且 lockfile 不含 registry 解析的 `@deepseek-ai/dsh-*`；若该断言不存在则新增之）。

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json test/release.test.ts patches/
git commit -m "chore(deps): 处理 alpha.3 新增 util 包与 sqlite 排除项"
```

---

## Task 5: 逐个 rebase 15 个组件补丁

对以下每个补丁重复本任务的步骤循环（**一个补丁一个 commit**）：
`cordis-plugin-loader`, `dsh-api-session-controller`, `dsh-client-modules`,
`dsh-client-ui-agent-preset`, `dsh-client-ui-chat`, `dsh-client-ui-deliverables`,
`dsh-client-ui-directory-picker-native`, `dsh-client-ui-layout`,
`dsh-client-ui-model-selection`, `dsh-client-ui-settings-models`,
`dsh-client-ui-sidebar`, `dsh-client-ui-trajectory`, `dsh-client-ui-workspace`,
`dsh-llm-deepseek`, `dsh-llm-pi-ai`。

**Files（每次一个）:**
- Rename: `patches/@deepseek-ai+<pkg>+0.1.2-alpha.1.patch` → `+0.1.2-alpha.3.patch`
- Test: 对应的 `test/*-patch.test.ts`（若有）

**Interfaces:**
- Consumes: Task 1 对应 tarball。
- Produces: 在 alpha.3 构建产物上干净应用的补丁。

- [ ] **Step 1: 装未打补丁的目标包**

```bash
rm -rf node_modules/@deepseek-ai/<pkg>
npm ci --ignore-scripts   # 或针对性重装
```

- [ ] **Step 2: 尝试旧补丁，观察失败**

```bash
git mv patches/@deepseek-ai+<pkg>+0.1.2-alpha.1.patch patches/@deepseek-ai+<pkg>+0.1.2-alpha.3.patch
npx patch-package @deepseek-ai/<pkg> --patch-dir patches --error-on-fail
```

Expected: 失败或部分 hunk 拒绝（行号 / hash 漂移），或干净应用（仅版本号改名即可）。

- [ ] **Step 3: 判定补丁去留**

对照上游 alpha.3 源码（`/Users/alex/Documents/Code/AI/deepseek-harness` 对应包）判断该补丁修的能力是否已被上游原生提供：
- **已原生提供**（如 alpha.1 时的 turnNavigation / access.confirm / directoryPicker 拆服务）→ `git rm` 补丁，在升级文档「交还上游」记一行，跳到 Step 6。
- **仍需要** → 继续 Step 4。

- [ ] **Step 4: 在解包目录里手动重做改动**

在 `node_modules/@deepseek-ai/<pkg>` 里按补丁原意重新编辑（跟随上游改名 / CSS module hash / 新增参数），然后：

```bash
npx patch-package @deepseek-ai/<pkg> --patch-dir patches
```

- [ ] **Step 5: 跑对应补丁测试**

```bash
npx vitest run test/<pkg>-patch.test.ts   # 若存在；否则 npx vitest run 相关用例
```

更新测试里硬编码的 `0.1.2-alpha.1` 路径与漂移的 hash / 行号断言。
Expected: PASS。

- [ ] **Step 6: Commit（单补丁）**

```bash
git add patches/ test/
git commit -m "fix(patch): <pkg> 适配 alpha.3"
```

---

## Task 6: `install-brand-assets.mjs` 前端资源匹配复核

**Files:**
- Modify: `scripts/install-brand-assets.mjs`
- Test: 相关 branding 测试（`test/branding-patch.test.ts` 或脚本自测）

**Interfaces:**
- Consumes: alpha.3 的 `@deepseek-ai/dsh-web-frontend` 前端资源。
- Produces: 成功改写品牌资源、目标缺失仍响亮失败的 postinstall 步骤。

- [ ] **Step 1: 比对 alpha.3 前端资源结构**

```bash
mkdir -p /tmp/wf-a3 && tar -xzf packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-dsh-web-frontend-0.1.2-alpha.3.tgz -C /tmp/wf-a3
grep -n "favicon" /tmp/wf-a3/package/**/index.html
cat /tmp/wf-a3/package/**/manifest*.json 2>/dev/null
```

对照 `scripts/install-brand-assets.mjs` 里的匹配字符串 / 标签选择器。

- [ ] **Step 2: 运行脚本观察结果**

```bash
node scripts/install-brand-assets.mjs
```

Expected: 成功，或在某匹配处响亮抛错。

- [ ] **Step 3: 若匹配落空，按「标签 / JSON 键」而非固定字符串修**

保持「目标缺失 → 抛错 → 中断 postinstall」的设计（alpha.1 教训：静默跳过会漏装 Electron）。

- [ ] **Step 4: 完整 postinstall 验证**

```bash
rm -rf node_modules && npm ci
```

Expected: 退出 0，Electron 二进制已装，品牌资源已改写。

- [ ] **Step 5: Commit**

```bash
git add scripts/install-brand-assets.mjs test/
git commit -m "fix(scripts): install-brand-assets 适配 alpha.3 前端资源"
```

---

## Task 7: 主进程运行时与移动端桥接复核

**Files:**
- Modify（按需）: `src/main/runtime/harness-runtime.ts`, `src/main/window-navigation.ts`, `src/main/mobile/lan-mobile-bridge.ts`
- Test: `test/lan-mobile-tunnel.test.ts`, `test/session-create-remote-event-patch.test.ts`, `test/query-rail-patch.test.ts`, `test/profile-compatibility.test.ts`

**Interfaces:**
- Consumes: alpha.3 `dsh web` 输出行格式、`/api/*` 端点、`/api/remote.mux` 流协议。
- Produces: 令牌捕获、就绪探测、桥接 RPC 与 alpha.3 对齐。

- [ ] **Step 1: 跑主进程相关测试基线**

```bash
npx vitest run test/lan-mobile-tunnel.test.ts test/session-create-remote-event-patch.test.ts test/query-rail-patch.test.ts test/profile-compatibility.test.ts test/gpu-fallback.test.ts
```

记录失败项。

- [ ] **Step 2: 比对 alpha.3 `dsh-web-app` / `dsh-client-connection` 源码**

在 `/Users/alex/Documents/Code/AI/deepseek-harness` 查：
- `dsh web:` 令牌输出行格式是否变（`harness-runtime.ts` 捕获正则）
- headless 进度是否仍在 stderr、就绪 / 错误归类字符串
- `/api/<namespace>/<method>` 端点、`args` 包裹、`session/page` 游标分页、`remote.mux` 流名 是否再变（`lan-mobile-bridge.ts`）
- `--trusted-host` 默认值对 `--host 127.0.0.1` 的影响

- [ ] **Step 3: 按 diff 调整实现**

改动处更新代码注释里的 `0.1.2-alpha.1` → `0.1.2-alpha.3`（`harness-runtime.ts:161,306`、`window-navigation.ts:16`、`lan-mobile-bridge.ts:46,794`）。

- [ ] **Step 4: 跑测试确认通过**

```bash
npx vitest run test/lan-mobile-tunnel.test.ts test/session-create-remote-event-patch.test.ts test/query-rail-patch.test.ts test/profile-compatibility.test.ts
```

Expected: PASS（或与 alpha.1 基线相同的已知例外）。

- [ ] **Step 5: Commit**

```bash
git add src/main/ test/
git commit -m "fix(main): 运行时与移动端桥接复核 alpha.3"
```

---

## Task 8: 全量测试 + typecheck + 版本字样清理

**Files:**
- Modify: `test/version-info.test.ts` 及其他残留硬编码 `0.1.2-alpha.1` 的测试
- Modify: `README.md` `README.zh.md` `README.ja.md` `README.es.md` `README.pt.md` `README.ru.md`

- [ ] **Step 1: 全量搜索残留版本字样**

```bash
grep -rn "0.1.2-alpha.1\|harness-0.1.2-alpha.1" --include="*.ts" --include="*.md" --include="*.mjs" --include="*.json" . | grep -v node_modules | grep -v packages/harness-0.1.2-alpha.3 | grep -v docs/harness-0.1.2-upgrade.md | grep -v docs/superpowers
```

逐条改为 `0.1.2-alpha.3`（`docs/harness-0.1.2-upgrade.md` 作为历史记录保留原文）。

- [ ] **Step 2: 全量测试**

```bash
npm test
```

Expected: 通过数 ≥ alpha.1 基线（454 tests / 61 files 附近，会因增删略变）；记录已知的 WebSocket teardown 退出码 1。

- [ ] **Step 3: typecheck**

```bash
npm run typecheck
```

Expected: 0 错误。

- [ ] **Step 4: build**

```bash
npm run build
```

Expected: ok。

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "chore: 清理 alpha.1 版本字样，全量测试通过"
```

---

## Task 9: 认证链路与 Electron 实机验证

- [ ] **Step 1: verify-harness-auth**

```bash
node scripts/verify-harness-auth.mjs
```

Expected: 401 → 303 → 200 全通过，含「不带 cookie 必须 401」。

- [ ] **Step 2: 实际启动 Electron**

```bash
npm run build && npx electron-vite preview
```

（或项目既有的启动方式；参照 `run` skill / `docs/development.md`。）
Expected: 应用正常运行，profile 中 4 个桌面插件（client-ui / preset-transfer / hmr-fallback / market-installer）全部挂载，无 `Cannot find package 'dsh-desktop-*'` 报错。

- [ ] **Step 3: 手动冒烟**

新建会话、发一条 prompt、打开设置 → 模型页、侧边栏品牌显示正确、手机入口在设置行最右。历史会话列表正常显示（验证 SQLite persistence 移除无影响）。

- [ ] **Step 4: 记录验证结果**（写入 Task 10 的升级文档，失败也照记）

---

## Task 10: 升级文档

**Files:**
- Create: `docs/harness-0.1.2-alpha.3-upgrade.md`
- Modify: `docs/harness-0.1.2-upgrade.md`（顶部加「本文档为历史记录，最新见 alpha.3」）

- [ ] **Step 1: 写 `docs/harness-0.1.2-alpha.3-upgrade.md`**

结构照 alpha.1 文档：
- **状态**：`npm ci` / `build` / `test`（数字）/ `typecheck` / `verify-harness-auth` / Electron 实机 的结果
- **本地打包**：Task 1 的命令与产物计数
- **上游结构性变更**：删 `session-persistence-sqlite`（+ 桌面端零影响说明）、删 `agent-spine-demo`（+ `dsh-sdk-minimal` 内联）、新增 `ui-schedule` / `util/*`（+ 闭包判定结论）
- **会话持久化**：JSONL-only，历史会话可见性不受影响，「旧版本导出」仅对上游 CLI sqlite 用户
- **主机 API / 桥接**：alpha.3 是否再变（Task 7 结论）
- **补丁清单**：新增 / 删除 / 仅偏移 三类表格（Task 3/5 结果）
- **踩到的问题**：构建 / 补丁 / 启动期
- **剩余工作**：上游发 npm 后换回语义化版本 + 重生成 lockfile（照抄 alpha.1 的 4 步）

- [ ] **Step 2: Commit**

```bash
git add docs/
git commit -m "docs: harness 0.1.2-alpha.3 升级记录"
```

---

## Self-Review

**Spec 覆盖检查：**
- 本地打包桥接 → Task 1 ✓
- 依赖切换 + lockfile → Task 2 ✓
- market-installer peer → Task 2 Step 4 ✓
- 删 spine-demo → Task 2 Step 2 ✓
- 新增 util 包闭包判定 → Task 4 ✓
- session-persistence-sqlite 排除项清理 → Task 4 Step 3 ✓
- dsh 闭包补丁 → Task 3 ✓
- 15 个组件补丁 rebase + 去留判定 → Task 5 ✓
- install-brand-assets → Task 6 ✓
- harness-runtime / 桥接 → Task 7 ✓
- 版本字样清理 → Task 8 ✓
- 全量验证（ci/build/test/tsc/auth/Electron）→ Task 8 + 9 ✓
- 升级文档 → Task 10 ✓
- README 更新 → Task 8 Step 1 ✓

**Placeholder 扫描：** Task 4 Step 2 / Task 5 Step 3-4 是判定分支而非固定代码——这是依赖升级任务的固有性质（补丁内容依赖尚未生成的 alpha.3 构建产物）。每个分支都给了明确的判定标准（「上游是否已原生提供」）和可执行命令，非占位符。

**类型一致性：** 无跨任务函数签名；`packages/harness-0.1.2-alpha.3/` 路径、补丁命名 `+0.1.2-alpha.3.patch`、`^0.1.2-alpha.3` peer 范围在全文一致。

**已知偏离标准 TDD 的地方：** 依赖 / 补丁类任务无法「先写失败测试再写实现」——改为「跑既有测试基线 → 改动 → 跑测试确认」，既有测试套件（`*-patch.test.ts`、`release.test.ts`、`desktop-plugin-closure.test.ts`）就是回归网。
