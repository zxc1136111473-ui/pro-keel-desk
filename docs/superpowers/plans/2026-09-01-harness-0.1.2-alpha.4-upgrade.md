# Harness 0.1.2-alpha.4 升级 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.

**Goal:** 把 dsh-desktop 从已适配的 Harness `dsh-v0.1.2-alpha.3` 推进到 `dsh-v0.1.2-alpha.4`，依赖 / 19 补丁 / 宿主脚本 / 移动端桥接 / 测试 / 文档全部适配并通过完整验证。

**Architecture:** 与 alpha.3 升级同形状——本地 clone 构建 alpha.4 tarball 桥接，`file:` 依赖切换，19 个 `patch-package` 补丁逐个 rebase，宿主侧按 alpha.4 实际行为复核（会话 branding 重构重点看移动端 wire）。

**Tech Stack:** Electron + electron-vite，Vitest，patch-package，npm（应用侧），pnpm@11.7.0 via corepack（上游构建），Node 24。

## Global Constraints

- 目标上游标签 `dsh-v0.1.2-alpha.4`（commit `4e84901e64`）。从 alpha.3 直接推进，跳过独立合并 alpha.3。
- 依赖来源：仓库内 `file:packages/harness-0.1.2-alpha.4/...` tarball，**不**换回语义化版本。
- 上游构建目录 `/Users/alex/Documents/Code/AI/deepseek-harness`（已 fetch alpha.4 标签，当前 detached 在 alpha.3）。
- 构建工具链 `corepack pnpm`；pack 必须 `corepack pnpm exec tsx scripts/release/pack.ts --family X`（`pnpm run release:pack -- --family X` 会报错）。
- 验证必须完整 `npm ci`，**禁止** `--ignore-scripts` 作最终验证。
- 保持排除上游未启用的 provider / 测试包（e2b、lsp、mock-server、replay、subagent-claude-code/codex、web-search-exa/perplexity、code-runtime-python 等）。
- 补丁命名 `@deepseek-ai+<pkg>+0.1.2-alpha.4.patch`（cordis-plugin-loader 用其 vendor 版本号）。`test/patch-path.ts` `patchPath()` 要求每包恰好 1 个 `.patch` 文件。
- 补丁去留标准：「上游是否已原生提供」→ 是则 `git rm` + 升级文档记「交还上游」。
- `test/desktop-plugin-closure.test.ts` 不变量：每个 `dsh-desktop-*` 行同时在 profile patch yml、`@deepseek-ai+dsh` 补丁、`package.json` `file:` 依赖三处。
- 4 个 CSS 补丁（agent-preset / model-selection / settings-models / workspace）hunk 里的 `//#region \0dsh-css:<绝对路径>` context 行——不要手改补丁体，沿用 alpha.3 已有的文档说明。
- **⚠️ worktree 隔离**：所有实施只在 `/Users/alex/Documents/Code/dataelem/dsh-desktop/.claude/worktrees/harness-alpha3-adapt-adfd61`（branch `claude/harness-alpha3-adapt-adfd61`）内操作。**绝不** `cd` 进主检出 `/Users/alex/Documents/Code/dataelem/dsh-desktop`（不同分支，用户 WIP）。每个 `git commit` / `npm ci` 前 `pwd` 确认。上游 harness repo 只读。
- 会话 branding 重构（`SessionSeq` / `SessionLogOffset`）是编译期 TS 变更；上游 Agent Note 明确 wire / v0 JSONL / API 数值表示不变。移动端桥接以实测为准。
- 完成定义：`npm ci` + `npm run build` + `npm test` + `npm run typecheck` + `node scripts/verify-harness-auth.mjs` + Electron 实机（4 插件挂载 + 会话历史分页冒烟）全绿。

---

## 文件结构

- **新增** `packages/harness-0.1.2-alpha.4/{npm-vendor,npm-dsh}/*.tgz` + `README.md`；**删除** `packages/harness-0.1.2-alpha.3/`
- `package.json`、`packages/dsh-desktop-{client-ui,preset-transfer,market-installer}/package.json`、`package-lock.json`
- `patches/` 19 个文件全部改名 + rebase（含 f13001b 新增的 session-persistence/-jsonl/dsh-workspace）
- `scripts/install-brand-assets.mjs`（按需）、`scripts/verify-harness-auth.mjs`（版本注释）
- `src/main/runtime/harness-runtime.ts`、`src/main/mobile/lan-mobile-bridge.ts`、`src/main/window-navigation.ts`（按需，多半只有注释）
- `test/`：`release.test.ts`、`desktop-plugin-closure.test.ts`、`*-patch.test.ts`、`preset-transfer-patch.test.ts`、`profile-compatibility.test.ts`、`readme-parity.test.ts`、`session-create-remote-event-patch.test.ts`、`lan-mobile-*.test.ts`、`open-in-finder.test.ts`
- `packages/dsh-desktop-preset-transfer/index.js`（`PRESET_SOURCE_DSH_VERSION`）
- 6 份 `README*.md`
- **新增** `docs/harness-0.1.2-alpha.4-upgrade.md`；`docs/harness-0.1.2-alpha.3-upgrade.md` 顶部交叉引用

---

## Task 1: 上游 alpha.4 构建与打包

**Files:** Create `packages/harness-0.1.2-alpha.4/**`; Delete `packages/harness-0.1.2-alpha.3/`

**Interfaces:** Produces `packages/harness-0.1.2-alpha.4/npm-{dsh,vendor}/*.tgz` 全量，供后续任务 `file:` 引用。

- [ ] **Step 1: 切 alpha.4 装依赖**
```bash
cd /Users/alex/Documents/Code/AI/deepseek-harness
git checkout dsh-v0.1.2-alpha.4
corepack pnpm install --frozen-lockfile
```
Expected: 成功。若 frozen 失败报 BLOCKED，不自行降级。

- [ ] **Step 2: 构建 official**
```bash
corepack pnpm run build:official
```
Expected: 退出 0。

- [ ] **Step 3: 打包**
```bash
corepack pnpm exec tsx scripts/release/pack.ts --family vendor --out dist/npm-vendor --concurrency 8
corepack pnpm exec tsx scripts/release/pack.ts --family dsh    --out dist/npm-dsh    --concurrency 8
```
记录 vendor / dsh tarball 数（alpha.3 为 9 + 244；alpha.4 净 −1 包，预期 dsh ≈ 243）。

- [ ] **Step 4: 拷入 worktree 并删旧目录**
```bash
cd /Users/alex/Documents/Code/dataelem/dsh-desktop/.claude/worktrees/harness-alpha3-adapt-adfd61
pwd   # 确认在 worktree
rm -rf packages/harness-0.1.2-alpha.3
mkdir -p packages/harness-0.1.2-alpha.4
cp -R /Users/alex/Documents/Code/AI/deepseek-harness/dist/npm-vendor packages/harness-0.1.2-alpha.4/
cp -R /Users/alex/Documents/Code/AI/deepseek-harness/dist/npm-dsh    packages/harness-0.1.2-alpha.4/
```

- [ ] **Step 5: README** —— 照 alpha.3 的 `packages/harness-0.1.2-alpha.3/README.md` 结构，上游标签 `dsh-v0.1.2-alpha.4` + commit + 4 步命令 + 产物计数 + 「上游发 npm 后删除本目录」。顶部指向 `../../docs/harness-0.1.2-alpha.4-upgrade.md`。

- [ ] **Step 6: 包 delta 记录**
```bash
comm -3 \
  <(git show HEAD:package.json | grep -oE 'deepseek-ai-dsh[a-z-]*-0.1.2-alpha.3' | sed 's/-0.1.2-alpha.3//' | sort -u) \
  <(ls packages/harness-0.1.2-alpha.4/npm-dsh | sed 's/-0.1.2-alpha.4.tgz//' | sort -u)
```
预期：`dsh-tool-subagent-report` 消失（本就排除）；无 dsh-desktop 已引用包被删。vendor 段版本号逐一记录。

- [ ] **Step 7: Commit**
```bash
git add packages/harness-0.1.2-alpha.4 packages/harness-0.1.2-alpha.3
git commit -m "chore(harness): 打包上游 dsh-v0.1.2-alpha.4 本地 tarball"
```

---

## Task 2: package.json 依赖切换 + peer + lockfile

**Files:** `package.json`, `packages/dsh-desktop-{client-ui,preset-transfer,market-installer}/package.json`, `package-lock.json`

**Interfaces:** Consumes Task 1 tarball。Produces 可 `npm ci` 解析、全 `file:` 的依赖图。

- [ ] **Step 1: 批量替换 dsh 路径**
`package.json` 每处 `file:packages/harness-0.1.2-alpha.3/npm-dsh/deepseek-ai-<x>-0.1.2-alpha.3.tgz` → `.../harness-0.1.2-alpha.4/npm-dsh/deepseek-ai-<x>-0.1.2-alpha.4.tgz`。逐一确认目标 tarball 存在于 `packages/harness-0.1.2-alpha.4/npm-dsh/`。

- [ ] **Step 2: vendor 段替换**
`npm-vendor` 段（`@deepseek-ai/cordis`、`cordis-plugin-*`、`cosmokit`、`schemastery`）：目录名换 + 版本号按 `ls packages/harness-0.1.2-alpha.4/npm-vendor/` 实际文件名跟随（可能未变）。

- [ ] **Step 3: 删上游已移除依赖（Task 1 已确认）**
删除 `package.json` 的 `"@deepseek-ai/dsh-tool-subagent-report": "file:..."` 行（约第 245 行）——
alpha.4 上游删除了该包，`packages/harness-0.1.2-alpha.4/npm-dsh/` 无对应 tarball，不删会导致 `npm install` 解析失败。
代码中若有 `dsh-tool-subagent-report` 引用一并处理（`grep -rn "tool-subagent-report" src/ build/ packages/dsh-desktop-*/`）。
`dsh-code-runtime-python` 本就不是依赖（在 `test/release.test.ts:103` 排除清单），无需动 package.json。

- [ ] **Step 4: peer 范围**
`packages/dsh-desktop-{client-ui,preset-transfer,market-installer}/package.json` 的每个 `@deepseek-ai/dsh*` peer / dependencies 条目 `^0.1.2-alpha.3` → `^0.1.2-alpha.4`。`@deepseek-ai/cordis` `^4.x` 不动。

- [ ] **Step 5: 重生成 lockfile**
```bash
pwd   # worktree
rm -rf node_modules package-lock.json
npm install --ignore-scripts
grep -c "harness-0.1.2-alpha.3" package-lock.json          # = 0
grep -nE '"resolved": "https?://[^"]*deepseek-ai[/-]dsh' package-lock.json   # 空
```

- [ ] **Step 6: 完整 npm ci**
```bash
rm -rf node_modules && npm ci
```
Expected: 退出 0。`patch-package` 阶段会报未适配的补丁——记录失败补丁名（进 Task 3）。仅依赖解析失败才在本任务修。

- [ ] **Step 7: Commit**
```bash
git add package.json package-lock.json packages/dsh-desktop-*/package.json
git commit -m "chore(deps): 依赖切换到 harness 0.1.2-alpha.4"
```

---

## Task 3: `@deepseek-ai/dsh` 闭包补丁

**Files:** `patches/@deepseek-ai+dsh+0.1.2-alpha.3.patch` → `+0.1.2-alpha.4.patch`; `test/desktop-plugin-closure.test.ts`（按需）

- [ ] **Step 1** 解包 `packages/harness-0.1.2-alpha.4/npm-dsh/deepseek-ai-dsh-0.1.2-alpha.4.tgz` 到 `/tmp`，读 `package.json` 的 `dependencies`，与 alpha.3 版本对比。
- [ ] **Step 2** `npx vitest run test/desktop-plugin-closure.test.ts` —— 观察失败。
- [ ] **Step 3** `git mv` 改名。`npm ci --ignore-scripts` 拿 pristine dsh，手动加回 4 个 `dsh-desktop-*` 依赖行（位置照旧），`npx patch-package @deepseek-ai/dsh --patch-dir patches` 重生成（若 patch-package regen 因 `file:` 失败，用 `git diff --no-index` 手工生成并修 header 路径）。
- [ ] **Step 4** 测试内若硬编码 `0.1.2-alpha.3` 路径改为 `alpha.4`（多半版本无关，先查）。
- [ ] **Step 5** `npx vitest run test/desktop-plugin-closure.test.ts` → PASS。
- [ ] **Step 6** Commit `fix(patch): dsh 闭包补丁适配 alpha.4`。

---

## Task 4: sqlite / subagent-report 排除项

**Files:** `test/release.test.ts`, `package-lock.json`（若需）

- [ ] **Step 1** `test/release.test.ts` `excludedHarnessPackages`：确认 `@deepseek-ai/dsh-tool-subagent-report` 是否在该清单——**它其实不在**（Task 1 确认它曾是直接依赖，由 Task 2 删除）。若测试有其它地方引用它则清理。
- [ ] **Step 1b** `@deepseek-ai/dsh-code-runtime-python`（清单第 103 行）：alpha.4 上游把它移到 `packages/experimental/`，不再进 `dsh` family tarball 集。排除断言 `expect(packageJson.dependencies[name]).toBeUndefined()` + lockfile 断言仍成立（它本就不是依赖）——保留该行，加注释说明现状（上游已移到 experimental，桌面端继续不打包）。
- [ ] **Step 1c** `test/release.test.ts` 约 76-81 行的「vendors upstream-new closure packages」用例里有 2 处硬编码正则 `/^file:packages\/harness-0\.1\.2-alpha\.3\/npm-dsh\/...` —— 改为 `alpha\.4`（alpha.3 Task 4 加的闭包包来源断言，Task 3 已确认此处 fail）。
- [ ] **Step 2** 复核 `dsh-storage-sqlite` / `dsh-session-persistence-sqlite` 现状注释仍准确（alpha.3 遗留）。
- [ ] **Step 3** 若上游新增了应排除的 provider 包，加进清单。全仓再 `grep -rn "harness-0.1.2-alpha.3\|0.1.2-alpha.3" test/ | grep -v "// "` 扫一遍非注释的硬编码，本任务能顺手清的清（其余归 Task 8）。
- [ ] **Step 4** `npx vitest run test/release.test.ts test/desktop-plugin-closure.test.ts` → PASS。
- [ ] **Step 5** Commit `chore(test): alpha.4 排除清单 + 闭包包断言更新`。

---

> **补丁总数 = 19**（不是 16）。分支在 alpha.3 完成后被另一位开发者 commit `f13001b`
> "feat(session): port permanent deletion to alpha.3" 扩充：新增 3 个补丁
> （`dsh-session-persistence`、`dsh-session-persistence-jsonl`、`dsh-workspace`）、
> 大幅扩写 2 个补丁（`dsh-api-session-controller` +1084、`dsh-client-ui-workspace` +637）、
> 新增 `test/session-delete-patch.test.ts`。**alpha.4 必须一并把这 19 个带过去。**
>
> Task 2 的 `npm ci` 实测：8 个 apply 失败（需真 rebase），11 个仅版本号警告（改名 + 可能行号）。

### 永久删除功能（f13001b）跨补丁怎么落
- `dsh-session-persistence` / `-jsonl`：`PersistenceCoordinator.delete()`、`SessionPreparations.assertDeletable()`、backend `deleteStored`。**正落在 alpha.4 会话 branding 重构区**（`SessionSeq`/`SessionLogOffset`、`SessionPreparations`、`PersistenceCoordinator`）——重点。
- `dsh-api-session-controller`：删除 RPC（`session/delete` 之类），释放活动 Agent，拒绝子 Agent / capability 持有的会话。
- `dsh-workspace`：删除后清理会话索引。
- `dsh-client-ui-workspace`：删除前确认弹窗（工作区文件保留、不可撤销）+ 错误展示。
- `test/session-delete-patch.test.ts`（101 行）：锁这些补丁的关键字符串，验证时必须过。

---

## Task 5a: 10 个"仅版本号警告"补丁改名 + 按需 regen

范围（Task 2 实测 apply-clean，仅 patch 文件名版本不符）：
`cordis-plugin-loader`（→ `+1.0.3.patch`，vendor 版本未变，仅 alpha 无关；实际**无需改名**，确认即可）、
`dsh-client-modules`、`dsh-client-ui-directory-picker-native`、`dsh-client-ui-layout`、
`dsh-client-ui-sidebar`、`dsh-client-ui-trajectory`、`dsh-client-ui-agent-preset`、
`dsh-llm-deepseek`、`dsh-llm-pi-ai`、`dsh-workspace`。一补丁一 commit。

- [ ] **Step 1** `npm ci --ignore-scripts`（拿到已应用这批 + dsh 闭包的树）。
- [ ] **每个补丁**：`git mv patches/@deepseek-ai+<pkg>+0.1.2-alpha.3.patch → +0.1.2-alpha.4.patch`
  （`cordis-plugin-loader+1.0.3.patch` 名字不含 alpha，**不改名**，只在 Step 3 确认它仍 apply）
  → 拿该包 pristine → `npx patch-package <pkg> --patch-dir patches` 试应用
  → 仅 blob-index / 行号漂移则重生成（patch-package regen 对 `file:` 失败时用 `git diff --no-index` 并修 header）
  → 补丁体变化超出行号：读上游 alpha.4 源码确认意图仍成立；`ui-trajectory`（上游 `TrajectoryView.tsx` +69）、
    `ui-agent-preset`、`dsh-workspace` 重点复核目标代码是否还在 / 被上游原生化 → 代码消失则为去留候选，**上报不自删**。
- [ ] **Step 2** 相关测试硬编码 patch 路径更新：`test/client-modules-resolution-patch.test.ts`、
  `test/cordis-plugin-loader-patch.test.ts`、`test/directory-picker.test.ts`、`test/branding-patch.test.ts`、
  `test/provider-error-patch.test.ts`（部分）、`test/session-delete-patch.test.ts`（若读 `dsh-workspace` 补丁）。
  `patchPath()` 版本无关——多数无需改，先 grep 确认。
- [ ] **Step 3** 全部完成后 `rm -rf node_modules && npm ci` —— 这 10 个 + dsh 闭包干净应用无警告，剩余 error 恰为 5b 的 8 个。
- [ ] **Step 4** Commit（各 `fix(patch): <pkg> 适配 alpha.4`）。

---

## Task 5b-1: `dsh-api-session-controller` + `dsh-client-ui-chat`

`dsh-api-session-controller`（**最大补丁**：provider 错误 JSON-safe projection 剥离 + f13001b 的删除 RPC，
+1084 行）；`dsh-client-ui-chat`（provider QUOTA/FORBIDDEN 分类 + i18n，3 hunk）。

- [ ] `git mv` 两个补丁。拿 pristine alpha.4 `lib/*.js`。
- [ ] **api-session-controller** 分两块重放：
  - provider 块：`const { projections: _projections, ...remoteSummary } = this.listState.summaryFor(session)` +
    `ctx.emit("api-session/added", remoteSummary)`（alpha.4 pristine 是否仍发原始 summary？会话 branding 重构
    是否改了 `summaryFor` 形状？）
  - 删除 RPC 块：跟随 alpha.4 的 controller 结构 + 会话 branding（`SessionSeq` 等），释放 Agent / 拒绝子 Agent 逻辑保留
  - 去留：alpha.4 是否原生提供 session 删除 RPC？若是则该块交还上游。
- [ ] **ui-chat**：去留（alpha.4 是否原生 QUOTA/FORBIDDEN 分类？仍只 AUTH 则保留）→ 重放 → i18n key / zh·en 文案逐字。
- [ ] 测试全绿：`test/provider-error-patch.test.ts`、`test/session-create-remote-event-patch.test.ts`、
  `test/session-delete-patch.test.ts`（删除 RPC 部分）。上游 API 真改名才动测试。
- [ ] Commit（各一）。

---

## Task 5b-2: `dsh-client-ui-workspace`（unread + Finder + 永久删除 UI）

f13001b 后此补丁承载：未读标记（27 hunk）+ Open in Finder + 删除确认弹窗/错误展示。上游 alpha.4
`Rows.module.css` + `WorkspaceBrowser.module.css` 都有 diff，CSS hash 会再迁移（alpha.3 为 `_zvJpq_`）。

- [ ] `git mv` → grep 上游确认 unread / 删除确认 UI 无原生（`grep -ri "unread\|markSession\|confirmDelete\|工作区文件" <upstream>/packages/client/ui-workspace/src/`）
- [ ] 取 alpha.4 新 CSS hash，重取 base CSS 串
- [ ] 重放全部 hunk：unread（store / SessionTree·FlatList 参数 / 行 prop / 右键菜单 / i18n `标为未读`·`标为已读`）
  + Finder + 删除确认（工作区文件保留 / 不可撤销文案，错误展示）
- [ ] 去留：分类逐块判，任何被上游原生化的删补丁并记录
- [ ] 测试全绿：`test/workspace-unread-patch.test.ts`、`test/query-rail-patch.test.ts`、
  `test/open-in-finder.test.ts`（其断言的 workspace patch 名 `+0.1.2-alpha.3.patch` → `+0.1.2-alpha.4.patch`）、
  `test/session-delete-patch.test.ts`（UI 部分）
- [ ] Commit。

---

## Task 5b-3: `dsh-client-ui-model-selection`（可搜索模型列表，CSS hash）

- [ ] `git mv` → 确认「可搜索模型列表」上游仍无原生 → 取 alpha.4 新 hash（alpha.3 为 `Ydf5zq_`）→ 机械 hash swap + 行号，13 hunk 逻辑不变 → 生成 → 应用无错。
- [ ] `test/model-selection-search-patch.test.ts` PASS（`filterModelGroups`、`IconSearchOutline16`、zh/en 文案 `搜索模型或服务商` 等逐字）。
- [ ] Commit。

---

## Task 5b-4: `dsh-client-ui-settings-models`（ProviderPicker，双 CSS hash）

- [ ] `git mv` → 去留复核（`submitLabelKey` alpha.3 已原生，alpha.4 确认仍是；ProviderPicker 卡片网格 / onboarding / 目录搜索 / 图片输入 / 推理等级 / sticky footer 是否原生）→ 取 alpha.4 两个 module hash（alpha.3 为 `A_4Mua_` / `_2CJT8a_`）→ 重放 35 hunk → 硬编码 `dshProvider*` 类不变 → 生成 → 应用无错。
- [ ] `test/model-picker-patch.test.ts`、`test/model-reasoning-efforts-patch.test.ts`、`test/model-settings-catalog-ux-patch.test.ts`、`test/onboarding-patch.test.ts` 全绿；hash 断言（如 `A_4Mua_customizedBody`）跟随更新。
- [ ] Commit。

---

## Task 5b-5: `dsh-client-ui-deliverables` + `dsh-session-persistence` + `dsh-session-persistence-jsonl` 收尾

`dsh-client-ui-deliverables`（alpha.3 是机械补丁，alpha.4 现 apply 失败——bundle 漂移）；
两个 session-persistence 补丁（f13001b 的永久删除 backend，**正落在 alpha.4 会话 seq/offset branding 重构区**）。

- [ ] **deliverables**：`git mv` → 读旧补丁意图 → 对 pristine alpha.4 重放（去留：能力是否原生化）→ 生成 → 应用无错。
- [ ] **session-persistence** / **-jsonl**：`git mv` → 对 pristine alpha.4 `lib/index.js` 重放
  `PersistenceCoordinator.delete()` / `assertDeletable()` / backend `deleteStored`。**重点**：
  alpha.4 的 `SessionPreparations` / `PersistenceCoordinator` 因 `SessionSeq`/`SessionLogOffset` branding
  可能改了方法签名 / `phase` 枚举 / cursor 类型——跟随调整，删除逻辑语义保持。
  去留：alpha.4 是否已原生提供 `delete` / `deleteStored`？若是则交还上游 + 记录。
- [ ] `rm -rf node_modules && npm ci` —— **全部 19 补丁干净应用，0 error**。
- [ ] 测试全绿：`test/session-delete-patch.test.ts`（全部）+ 上游 persistence 相关（`test/*persist*`、`test/*session*` 若有）。
- [ ] Commit（各一）。

---

## Task 6: install-brand-assets.mjs 复核

- [ ] 解包 `dsh-web-frontend` alpha.4 tarball，比对 `index.html` favicon link / `manifest.webmanifest` icon entry 结构与 `scripts/install-brand-assets.mjs` 的匹配逻辑。
- [ ] `node scripts/install-brand-assets.mjs`（在已 `npm ci` 的树上）—— 成功输出 brand assets，或响亮失败。
- [ ] 若匹配落空按「标签 / JSON 键」修，保持目标缺失时抛错。
- [ ] `npx vitest run test/branding-patch.test.ts` → PASS。
- [ ] 无 diff 则无 commit（记录「验证无需改动」）；有则 Commit。

---

## Task 7: 主进程 + 移动端桥接 + 分页（重点）

**Files（按需）:** `src/main/runtime/harness-runtime.ts`, `src/main/mobile/lan-mobile-bridge.ts`, `src/main/window-navigation.ts`, 相关 `test/`

- [ ] **Step 1** 基线：`npx vitest run test/lan-mobile-tunnel.test.ts test/lan-mobile-bridge.test.ts test/lan-mobile-pages.test.ts test/session-create-remote-event-patch.test.ts test/query-rail-patch.test.ts test/profile-compatibility.test.ts test/gpu-fallback.test.ts`。
- [ ] **Step 2** upstream diff alpha.3→alpha.4：
```bash
cd /Users/alex/Documents/Code/AI/deepseek-harness
git diff dsh-v0.1.2-alpha.3 dsh-v0.1.2-alpha.4 -- packages/client/connection/src packages/api/session-controller/src packages/dsh-web-app/src | grep -E '^\+|^-' | grep -iE 'token|web:|--host|--trusted-host|remote.mux|events.mux|session/page|throughSeq|fromSeq|\$events|clientId|SessionSeq|SessionLogOffset|cursor' | head -80
```
- [ ] **Step 3** 逐一确认（会话 branding 重构不应泄漏到 wire）：
  - `dsh web:` 令牌输出行格式
  - headless stderr 就绪 / 错误归类
  - `/api/<ns>/<method>` 端点全集、`args` 包裹
  - **`session/page` 分页**：`throughSeq` / `fromSeq` 在 wire 上是否仍为普通 number；游标语义有无变化
  - `/api/remote.mux` 流名、`$events` / `workspace/follow` 订阅、`$events/result` 应答 `clientId`
  - `--trusted-host`
- [ ] **Step 4** 有行为变化则最小改 `src/main/**` + 对应测试；仅版本注释漂移则不动（alpha.3 已确认「Since 0.1.2-alpha.1」注释是准确历史记述）。
- [ ] **Step 5** `npx vitest run` 上述测试全套 → PASS。`npm run typecheck` → 0。
- [ ] **Step 6** Commit（`fix(main): …` 或 `no commit — nothing to change`）。
- [ ] **Step 7** 报告里明确写：会话分页 / 桥接 wire 是否受 branding 重构影响的结论 —— Task 9 实机分页冒烟据此重点验。

---

## Task 8: 版本字样清理 + 全量验证

- [ ] `packages/dsh-desktop-preset-transfer/index.js` `PRESET_SOURCE_DSH_VERSION` `'0.1.2-alpha.3'` → `'0.1.2-alpha.4'`。
- [ ] `test/preset-transfer-patch.test.ts` 3 处运行时 `'0.1.2-alpha.3'` → `'0.1.2-alpha.4'`（行 31 历史注释不动）。
- [ ] `test/profile-compatibility.test.ts` `version` / `expectedVersion` `'0.1.2-alpha.3'` → `'0.1.2-alpha.4'`。
- [ ] 6 份 `README*.md`：`@deepseek-ai/dsh@0.1.2-alpha.3` → `@0.1.2-alpha.4`（grep 每份确认）。`test/readme-parity.test.ts` `requiredFacts` token 跟随。
- [ ] 最终 sweep：`grep -rn "0.1.2-alpha.3" --include=*.ts --include=*.js --include=*.json --include=*.md --include=*.mjs . | grep -vE "node_modules|packages/harness-0.1.2-alpha.4|docs/harness-0.1.2-(upgrade|alpha.3-upgrade).md|.superpowers|docs/superpowers|package-lock"` —— 剩余全为历史注释，列进报告。
- [ ] `pwd`（worktree）→ `rm -rf node_modules package-lock.json && npm install` → `grep -c harness-0.1.2-alpha.3 package-lock.json` = 0 → `rm -rf node_modules && npm ci`（19 补丁 ✔）。
- [ ] `npm run typecheck` → 0。`npx vitest run` → 全绿（记录数）。`npm run build` → ok。
- [ ] Commit `chore: 清理 alpha.3 版本字样，peer 范围对齐 alpha.4`。

---

## Task 9: 认证链路 + Electron 实机 + 分页冒烟

- [ ] `node scripts/verify-harness-auth.mjs` → 令牌 → 401 → 303 → 200。
- [ ] `pwd`（worktree）`npm run build` → 用项目自身工具启动（`electron-vite preview` 或 `package:dev:dir` 后运行 `dist/` 二进制），cwd = worktree 根，捕获启动日志 ~40s。
- [ ] 启动日志确认：harness web server 起、**4 个桌面插件全部挂载**、主窗口加载前端、无 `Cannot find package` / `ERR_MODULE_NOT_FOUND` / plugin-recovery。
- [ ] **会话分页冒烟**（用户要求）：新建会话、发一条 prompt、打开历史 / 会话列表翻页，确认不塌、`session/page` 请求 200。移动端桥接路径若可触发一并验。
- [ ] 失败 → 捕获精确错误，判断是 alpha.4 适配 gap 还是环境问题，报告 DONE_WITH_CONCERNS / BLOCKED，不自行改码。
- [ ] 无 commit（验证任务）。结果写报告。

---

## Task 10: 升级文档

- [ ] 新建 `docs/harness-0.1.2-alpha.4-upgrade.md`，对齐 alpha.3 文档结构：
  - **状态**块（各验证结果 + 数字）
  - **本地打包**命令 + 产物计数
  - **上游结构性变更**：会话 seq/offset branding 重构（+ wire 不受影响的结论与证据）、`tool-subagent-report` 删除、`code-runtime-python` 路径移动、ui-* 普遍重建
  - **移动端桥接 / 分页**：Task 7 + Task 9 的验证结论
  - **补丁清单**：新增 / 删除 / 仅偏移 / 实质 rebase 四类表（含各 CSS hash alpha.3→alpha.4 迁移）
  - **踩到的问题**
  - **剩余工作**（上游发 npm 后：换回语义化版本 + 删本地目录 + registry 重生成 lockfile + 重跑验证）
- [ ] `docs/harness-0.1.2-alpha.3-upgrade.md` 顶部加一行指向 alpha.4 文档。
- [ ] Commit `docs: harness 0.1.2-alpha.4 升级记录`。

---

## Task 11: PR #264 更新

- [ ] `gh pr edit 264 --title "适配 Harness 0.1.2-alpha.4" --body ...`（正文更新为 alpha.4 的改动摘要 + 验证结果 + 剩余工作；说明本 PR 从 alpha.3 直接推进到 alpha.4，分支名 `harness-alpha3-adapt` 是历史遗留）。
- [ ] `git push`（分支已 tracking origin）。

---

## Self-Review

**Spec 覆盖**：本地打包→T1；依赖+peer+lockfile→T2；闭包补丁→T3；排除清单→T4；19 补丁→T3/T5a/T5b-1..5；install-brand-assets→T6；主进程+桥接+分页→T7；版本清理+全量验证→T8；auth+Electron+分页冒烟→T9；文档→T10；PR→T11。✓

**与 alpha.3 计划的差异**：结构性改动小得多（无新增包、只删 1 个已排除包）；新增「会话 branding 重构 wire 影响」这条主线，T7/T9 加了分页冒烟；ui-trajectory/ui-workspace 上游有实质改动，T5b-1/5b-2 标注需实质 rebase 而非纯 hash swap。

**Placeholder 扫描**：T5 系列是「判定 + 重放」分支而非固定代码——依赖升级任务固有性质，每步给了明确判定标准和命令。

**类型一致**：`packages/harness-0.1.2-alpha.4/` 路径、`+0.1.2-alpha.4.patch` 命名、`^0.1.2-alpha.4` peer 全文一致。
