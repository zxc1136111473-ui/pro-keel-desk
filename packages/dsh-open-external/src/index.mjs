// dsh-open-external — HOST half. This plugin's behaviour is entirely client-side
// (a link-click interceptor in the Harness iframe); the host exists only so the
// package keeps the standard two-half bundle shape. No routes, no state.
export const name = 'dsh-open-external'
export function apply() {}
