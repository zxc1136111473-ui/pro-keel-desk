# CONTEXT.md — 领域术语表

本文件只收录领域词汇的定义，不收录实现细节（实现见 README / AGENTS / CHANGELOG）。

## 分层记忆

- **L0（原始对话）**：清洗后的用户与助手消息原文，按天追加落盘；是记忆管线的输入，
  不进 UI 浏览器（量大且隐私敏感；模型侧可用 conversation_search 查询）。
- **L1（原子记忆）**：从 L0 蒸馏出的单条结构化事实，带类型 / 优先级 / 情境 / 版本。
  同类原子记忆会被去重合并（版本 +1）。
- **L2（场景块）**：把多条 L1 记忆按主题整合成的 Markdown 场景文件，带热度与摘要。
- **L3（画像）**：由场景蒸馏出的长期画像文档（分族 `persona-chat.md` / `persona-work.md`），读取时剥离场景导航段。

## 记忆生命周期

- **捕获**：把一轮对话清洗后记入 L0 的动作。
- **蒸馏**：L1 抽取、L1 去重合并、L2 场景整合、L3 画像更新的统称。
- **召回**：对话进行中把相关 L1 记忆与画像注入模型上下文的动作。
- **未蒸馏缓冲**：已捕获、尚未成功蒸馏的消息暂存区，按档位分桶、桶内按会话切片；
  既包括抽取失败的待重试消息，也包括攒够触发阈值之前的消息。
  缓冲本身持久化，重启不丢；重启恢复后自动补跑一次。
- **会话切片**：未蒸馏缓冲中按会话划分的消息组。蒸馏的一切触发都以切片为单位，
  切片内永不跨会话、跨档位混装。
- **捕获档位**：消息入桶时所在的会话档位。切片始终按捕获档位蒸馏，
  会话中途切换档位不重贴标签。
- **渐进阈值**：起步阶段蒸馏触发阈值从 1 条消息起、随每次成功抽取逐次翻倍至
  稳态值的机制——新用户首轮即出记忆，稳态后自动攒批省调用。
- **闲置兜底**：会话静默超过设定时长后，把该会话未蒸馏切片蒸馏掉的后台触发；
  接住"没攒够阈值用户就离开"的尾部，新鲜度损失有界。
- **情境（Scene）**：L1 抽取时对对话主题的切分与命名；L1 记录归属一个情境，
  L2 场景块按主题聚合多个情境的内容。

- **重建**：从 L0 事实源重新推导全部派生层（L1 → L2 → L3）的用户主动动作。
  L0 永不改动；旧派生层归档保留（不硬删）。重建统一按智能档蒸馏；
  重建分块执行，让位于正常对话轮次的蒸馏；结束后强制跑一轮 L2/L3。

## 召回注入

- **消息侧注入**：把召回的 L1 记忆作为一条独立消息送入对话历史的注入方式。
  注入消息带可辨识来源（宿主 UI 显示为"上下文注入 · memory"署名行）、对用户可见
  （构成"记忆生效"的显式提示），随对话历史留存，跨轮累积，随上下文压缩自然淘汰。
  _Avoid_: prependContext（上游 MemoryCore 术语，指混入用户消息本体的旧形态）
- **系统提示注入**：画像、场景导航、工具指南等逐轮稳定内容注入系统提示的方式。
  与消息侧注入相对——稳定内容走系统提示，动态召回走消息。
- **召回预算**：一次注入的长度约束——单条记忆截断上限与整轮总量上限；
  超限截断并提示模型可改用记忆工具查询全文。

## 记忆模式开关

- **总开关（记忆模式）**：关闭 = 捕获、蒸馏、召回全部停止；数据保留，重开后继续。
- **分项开关**：捕获 / 蒸馏 / 召回三个独立开关，仅在总开关开启时生效。
- **部署上限**：组合配置（cordis.patch.yml）里的静态 enabled 字段；运行时开关无法
  越过部署上限（生效 = 部署上限 AND 运行时开关）。
- **蒸馏模型**：蒸馏调用用的 LLM 路由（供应商 + 模型）。取值优先级：部署静态 pin
  （`llm.provider`+`llm.model` 双字段齐）> 运行时路由链的主路由行 > 宿主默认模型。
  部署 pin 时路由链编辑器只读（部署可强制蒸馏走指定路由）。
- **运行时路由链**：设置页统一路由列表的存储形态（`distillChain`）：第 1 行主路由
  （可留空跟随默认模型）、后续行为回退链、每行可带档位覆盖（缺省跟随部署全局）。
  非空即整体接管部署静态链；空 = 跟随部署配置。旧运行时键（单模型选择 + 全局
  档位）仍被兼容读取，UI 已不再写入。
- **主路由**：一次蒸馏调用首先尝试的路由，即"蒸馏模型"按上述优先级解析出的那一条。
- **回退链**：主路由失败后按序逐个尝试的备用路由列表（每条 = 供应商 + 模型 + 可选
  思考档位覆盖），条目顺序即降级优先级。**失败**指路由报错、被掐断、网络异常或
  空输出；调用方主动取消不算失败（不触发降级），输出解析失败也不在其列（那是
  成功调用之后的问题）。每条路由各享全额超时；全部失败才进入按会话退避。
  不配置回退链 = 行为与单路由完全一致。
  _Avoid_: fallback 链与"重试"混称——重试是同一路由再跑一次，回退是换下一条路由。
- **输出预算**：单个蒸馏层一次调用允许的最大输出 token 数（分层设护栏，模型跑偏
  时单次损失有界）。四层各自独立：L1 抽取 / L1 去重 / L2 场景 / L3 画像；设置页
  可运行时调整，0 = 跟随内置默认；思考档 high/xhigh/max 时生效限额自动 ×4（reasoning
  计入输出预算）。
- **输入预算**：单次蒸馏调用允许的最大输入字符数（≈token，中文 1 字≈1 token
  保守折算）。L1 抽取按此分块（不丢消息），L2/L3 超限截断；设置页可运行时调整，
  0 = 跟随部署配置 llm.maxInputChars。

## 会话记忆档位

- **档位**：每会话独立的记忆工作方式，四态——智能（auto）/ 日常（chat）/ 工作（work）/
  关闭（off）。显示名中文，配置键与英文层保留 auto/chat/work/off。
- **族（family）**：记忆的两个语义分区——chat（个人：persona/episodic/instruction）、
  work（工作：work_* 四类）。记录、场景、画像、checkpoint 都带族维度。
- **写入与召回同档**：档位同时决定蒸馏透镜（写哪族）与召回范围（读哪族）；
  智能档两族全开，关闭档对记忆系统完全隐身。
- **默认档**：新会话未显式选择时的档位，来自配置 `family`（默认 auto）；
  会话内一经选择即按 sessionId 持久化（session-modes.json）。
- **档位 vs 全局开关**：全局开关是总闸（横切所有会话），档位是总闸之下的会话级细分。

## 嵌入与检索

- **嵌入模型**：把文本映射为向量的编码网络（非生成式大模型）。分两类——**远程嵌入**
  （OpenAI 兼容服务，部署配置给定）与**本地嵌入模型**（用户从模型目录下载、插件进程内推理）。
  口语说的"向量模型"统一称嵌入模型；向量是它的输出，可下载的是模型本体。
- **嵌入源**：当前为记忆生成向量的来源，三态单选——远程 / 本地 / 关闭。关闭即纯关键词
  （BM25）检索。选择持久化在数据目录，重启不丢；受部署上限约束（部署未配远程则远程不可选，
  部署可禁止本地档）。
- **模型目录**：插件内置的可下载嵌入模型白名单，每款锁定版本与完整性校验和；
  用户从目录下载并启用其一，一个模型都不装则维持关键词检索（原有降级语义不变）。
- **下载**：把目录中选定的模型文件拉取到数据目录的动作；支持断点续传，
  完成后校验完整性并自动预热验证。单文件失败自动重试（换缓存键），
  可走代理（默认探测代理环境变量，与 curl/npm 同语义）。
- **运行时安装**：首次开启本地嵌入时，把推理运行时（一组钉死版本的普通依赖）
  安装到数据目录的动作；与**下载**（拉取模型文件）区分。受限环境安装失败
  则维持关键词检索并说明原因，不阻塞宿主。
- **预热**：首次需要嵌入时把已启用模型加载进内存的动作；加载后常驻，
  直到嵌入源切走或关闭才释放。
- **重嵌入**：切换嵌入源或模型（向量维度随之变化）后，对全部 L1 记忆与 L0 消息
  重算向量、重建向量表的后台动作。只重算向量，不改任何记忆内容——
  区别于**重建**（从 L0 重导全部派生层）。

## 记忆浏览器

设置页"记忆"区块的多 Tab 内容面板：概览（计数 + 开关 + 蒸馏路由链编辑器）、
记忆（L1 列表）、场景（L2 全文）、画像（L3 全文）、成本（token 看板）、
日志（诊断日志尾部）。只读为主（路由链编辑器是唯一编辑面）；L0 不入浏览器。
内容为两族混合视图。
