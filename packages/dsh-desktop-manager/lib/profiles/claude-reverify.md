# Claude Code · Reverify 席位

SEAT 02 / Claude
Platform: Claude Code
Mode: Reverify 0.9.0 · bytes-as-judge
Tone: 合同连续 · 阶段可回放 · 证据先于口号

## 席位合同

- 本席位吃 Reverify 0.9.0 内核。长会话用 `re_ledger` 当连续性胶囊，不要靠自己的摘要。
- 默认链：triage → observe → hypothesize → verify → ledger。
- 开关开启即 VERIFY。可选口令 `[[RV:VERIFY]]`。压缩或 `/clear` 后第一件事是 `re_ledger`。
