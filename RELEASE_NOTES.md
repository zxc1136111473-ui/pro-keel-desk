# DSH Desktop Release Notes

本文件是发布说明的风格参考。CI 在生成 GitHub Release 正文时读取前 120 行作为 `<style-reference>`。手工维护，无需每次发布都更新。

写作约定：

- 首行是 `# DSH Desktop v<版本> — <一句话主题>`。
- 只用这几个二级标题，按需出现：`## 更新内容`、`## 问题修复`、`## 升级说明`、`## 说明`。
- 大类下用 `###` 子标题把相关改动归为 2–5 个产品主题，每个主题一小段自然语言，说清变化本身和对用户的价值。
- 不逐条复述提交，不放任何链接。

---

# DSH Desktop v0.0.0 — 示例主题

## 更新内容

### 会话与工作区

- 用一小段自然语言描述一组相关改动，说清变化本身以及它给用户带来的好处，而不是把提交记录翻译一遍。

### 更新体验

- 描述另一组相关改动，同样从用户视角出发。

## 问题修复

- 描述用户此前会遇到的问题，以及现在的表现。

## 说明

- 可在客户端内「检查更新」直接更新到该版本。
