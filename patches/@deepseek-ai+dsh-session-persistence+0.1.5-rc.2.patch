diff --git a/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js b/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js
--- a/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js
+++ b/node_modules/@deepseek-ai/dsh-session-persistence/lib/index.js
@@ -262,6 +262,16 @@
 	constructor(ctx) {
 		super(ctx, "sessionPersistence");
 	}
+	/**
+	* Permanently remove one stored Session identity when the backend supports
+	* deletion. Backends that cannot delete keep this refusal.
+	* @param _id - the stored session to remove.
+	* @param _options - optional cancellation.
+	* @returns whether a durable artifact was removed.
+	*/
+	delete(_id, _options) {
+		return Promise.reject(/* @__PURE__ */ new Error("this session persistence backend does not support deletion"));
+	}
 };
 //#endregion
 export { SessionAlreadyExistsError, SessionAlreadyOwnedError, SessionFormatUnsupportedError, SessionHandleClosedError, SessionOwnershipLostError, SessionPersistence, SessionPersistence as default, SessionPersistenceCorruptionError, SessionPersistenceNotFoundError, SessionPersistenceRevision, SessionReadOnlyError, assertContiguous, assertStoredId, assertVersion, materializeAppendBatch, materializeCreateHeader, sessionFormatVersionRefusal, validateStoredEvents };
diff --git a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts
--- a/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts
+++ b/node_modules/@deepseek-ai/dsh-session-persistence/lib/types/index.d.ts
@@ -64,6 +64,11 @@
 /** Options for {@link SessionPersistence.stat}. */
 export interface SessionPersistenceStatOptions {
     /** Optional cancellation for backend metadata reads. */
+    readonly signal?: AbortSignal;
+}
+/** Options for {@link SessionPersistence.delete}. */
+export interface SessionPersistenceDeleteOptions {
+    /** Optional cancellation observed before backend work starts. */
     readonly signal?: AbortSignal;
 }
 /** Options for {@link SessionPersistence.list}. */
@@ -153,6 +158,18 @@
      * @returns one snapshot per stored session.
      */
     abstract list(options?: SessionPersistenceListOptions): Promise<readonly SessionPersistenceSnapshot[]>;
+    /**
+     * Permanently remove one stored Session identity.
+     *
+     * Deletion is optional: the base implementation refuses, and a backend that
+     * can delete overrides it. Refuses while this process holds write ownership
+     * of the session, so a live writer never loses its artifact underneath it.
+     * @param _id - the stored session to remove.
+     * @param _options - optional cancellation.
+     * @returns whether a durable artifact was removed; `false` when the session
+     *   was already absent.
+     */
+    delete(_id: SessionId, _options?: SessionPersistenceDeleteOptions): Promise<boolean>;
 }
 export default SessionPersistence;
 //# sourceMappingURL=index.d.ts.map